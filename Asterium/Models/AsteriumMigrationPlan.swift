import Foundation
import SwiftData

// Migration plan intentionally disabled.
// Pathwork keeps the original `nextSteps: String` field untouched,
// and the new UI uses the additive `nextStepsList: [String]` field instead.

